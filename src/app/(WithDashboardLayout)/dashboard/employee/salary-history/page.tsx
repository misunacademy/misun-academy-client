export const instant = false

import EmployeeSalaryhistoryClient from "./_components/EmployeeSalaryhistoryClient"

export default function Page(props: Record<string, unknown>) {
  return <EmployeeSalaryhistoryClient {...props} />
}
