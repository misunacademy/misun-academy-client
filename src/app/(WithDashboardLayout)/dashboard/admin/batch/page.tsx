import BatchDashboard from '@/components/module/batch/Batch';
import React from 'react';

const page = () => {
    return (
        <div>
            <BatchDashboard />
        </div>
    );
};

export default page;