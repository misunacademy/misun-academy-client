# ================================
# BETTER AUTH REFACTORING - FRONTEND ENV
# ================================

# Backend API URL
NEXT_PUBLIC_BASE_API_URL=http://localhost:5000

# Frontend Site URL (for OAuth callbacks and absolute URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ===================================
# BETTER AUTH (NEW - SIMPLIFIED)
# ===================================
# Better Auth endpoint is now pointing to backend
# No need for NEXT_PUBLIC_AUTH_URL, MONGODB_URI, or AUTH_SECRET in frontend
# All auth logic is handled by the backend

# ===================================
# REMOVED VARIABLES (No longer needed)
# ===================================
# NEXT_PUBLIC_AUTH_URL=http://localhost:3000/api/auth (REMOVED - backend handles auth)
# MONGODB_URI=mongodb://localhost:27017/misun-academy (REMOVED - backend only)
# AUTH_SECRET=your-secret (REMOVED - backend only)
# DB_NAME=misun-academy (REMOVED - backend only)
# GOOGLE_CLIENT_ID=... (REMOVED - backend only)
# GOOGLE_CLIENT_SECRET=... (REMOVED - backend only)

# ===================================
# OTHER FRONTEND VARIABLES
# ===================================
# Add any other frontend-specific env vars here
# For example:
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEXT_PUBLIC_GA_TRACKING_ID=UA-XXXXXXXXX-X

# ===================================
# SETUP INSTRUCTIONS
# ===================================
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Update NEXT_PUBLIC_BASE_API_URL to match your backend URL
#    - Development: http://localhost:5000
#    - Production: https://api.yourdomain.com
#
# 3. Install dependencies:
#    npm install
#
# 4. Start development server:
#    npm run dev
#
# 5. Build for production:
#    npm run build
